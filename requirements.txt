# Pinned for reproducible deploys — a surprise minor bump shouldn't reach prod
# untested. Bump deliberately and let CI verify.
python-telegram-bot==21.11.1
httpx==0.28.1
python-dotenv==1.2.2
Pillow==12.3.0
